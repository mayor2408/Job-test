# Все решения заданий

## Блок 1: Теория вероятности и логика
**Задание 1: Фермер**
Ответ: 3,99
Логика: Математическое ожидание суммы индикаторов для каждого из 6 видов E = 6 * (1 - (5/6)^6).

**Задание 2: Кулинарное соревнование**
Ответ: 20,0
Логика: Шанс одного повара выиграть оба раунда равен 1/2 * 1/2 = 1/4. Матожидание для 80 участников: 80 * 0,25.

**Задание 3: Одинокая дорога**
Ответ: 63,2; 93,3
Логика: Вероятность 0 машин за 30 минут = 0,05. Вероятность 0 машин за 1 минуту: p = 0,05^(1/30).
За 10 минут: 1 - 0,05^(10/30) ≈ 0,6316.
За 27 минут: 1 - 0,05^(27/30) ≈ 0,9326.

---

## Блок 2: Python
**Задание 1: Изоморфизмы**
def is_isomorphic(s: str, t: str) -> bool:
    if len(s) != len(t):
      return False
    return len(set(s)) == len(set(t)) == len(set(zip(s, t)))
Время: O(N), Память: O(N)

**Задание 2: Натуральная последовательность**
def missing_number(nums: list) -> int:
    n = len(nums) + 1
    return n * (n + 1) // 2 - sum(nums)
Время: O(N), Память: O(1)

**Задание 3: Факторизация**
def prime_factors(n: int) -> list:
    factors, d = [], 2
    while d * d <= n:
        while n % d == 0:
            factors.append(d)
            n //= d
        d += 1
    if n > 1:
        factors.append(n)
    return factors
Время: O(sqrt(N)), Память: O(log N)

---

## Блок 3: SQL
**Задание 1: Абитуриенты**
SELECT 
    id, 
    scores, 
    RANK() OVER (ORDER BY scores DESC) as rank
FROM examination;

**Задание 2: FULL JOIN**
Ответ: минимально 30 и максимально 600 строк.
Логика: 30 строк — если 20 ключей совпали 1 к 1. 600 строк — декартово произведение (дубликаты).

**Задание 3: Покупки**
SELECT a.client_id
FROM account a
JOIN transaction t ON a.id = t.account_id
WHERE t.transaction_date >= CURRENT_DATE - INTERVAL '1 month'
GROUP BY a.client_id
HAVING SUM(t.amount) < 5000;

---

## Блок 4: Статистика и АБ-тесты
**Задание 1:** Верные утверждения 3 и 4.
**Задание 2:** Верное утверждение: Вероятность наблюдения такого результата при условии верности нулевой гипотезы.
**Задание 3:** Да, при 5 млн выборки работает ЦПТ, t-критерий Стьюдента применим.

---

## Блок 5: ML Base
**Задание 1:** Вторую модель, инвертировав предсказания (p на 1-p). ROC-AUC станет 0.9.
**Задание 2:** Ответ: 0,75. Логика: 42 верные пары из 56.
**Задание 3:** Ответ: -0,85. Вывод: Сильная отрицательная связь, но корреляция не означает каузальность.